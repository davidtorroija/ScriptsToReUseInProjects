/*dms: once plugin return elements just once*/
(function ($) {
    $.extend($.fn, {
        once: function (options) {
            return this.filter('[once-done!=done]').attr('once-done', 'done');
        }
    });

})(jQuery);
